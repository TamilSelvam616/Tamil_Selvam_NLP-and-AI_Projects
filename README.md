# PRACTICAL – 12 (i) Breadth First Search (BFS)

# Aim:

To implement Breadth First Search (BFS) algorithm for traversing a graph in level-wise manner.

# Procedure:

Represent the problem as a graph using nodes and edges.

Select a start node from where traversal will begin.

Initialize an empty list to store visited nodes.

For BFS, use a queue data structure to store nodes to be explored.

Insert the start node into the queue.

Remove the front element of the queue and mark it as visited.

Insert all unvisited adjacent nodes into the queue.

Repeat steps 6 and 7 until the queue becomes empty.

Save the program for AI problem solving references.

# Result:
Program executed successfully.

# 
# PRACTICAL – 12 (ii) Depth First Search (DFS)

# Aim:

To implement Depth First Search (DFS) algorithm for traversing a graph depth-wise.

# Procedure:

Represent the problem as a graph using nodes and edges.

Select a start node from where traversal will begin.

Initialize an empty list to store visited nodes.

For DFS, use recursion or stack to traverse nodes deeply.

Start DFS from the start node and mark nodes as visited.

Recursively visit all unvisited adjacent nodes.

Continue until all nodes are visited.

Display the traversal order of nodes.

Compare BFS and DFS results.

Save the program for AI problem solving references.

# Result:

Program executed successfully.
